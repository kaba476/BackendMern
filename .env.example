# Copier ce fichier en .env et remplir les valeurs.
# Ne jamais commiter .env (il doit rester dans .gitignore).

# Connexion MongoDB
MONGO_URI=mongodb://localhost:27017/gestion_school

# JWT (clé secrète pour signer les tokens — utiliser une chaîne longue et aléatoire en production)
JWT_SECRET=12345

# Port du serveur (optionnel, défaut 5000)
PORT=5000

